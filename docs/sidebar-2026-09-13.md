# Sidebar: vzhled a sbalování — 13. 9. 2026

Postranní panel má sjednocené ikony, jemné oddělovače, zvýrazněnou aktivní sekci a samostatnou patičku s účtem. Tlačítko pod logem přepíná šířku 256 / 76 px. Sbalení poskytne obsahu dalších 180 px. Názvy ikon se zobrazují při najetí i zaměření klávesnicí; Escape popisek zavře.

Volba se ukládá jako `0` / `1` pod `bohemka.sidebar.collapsed`. Uložení neobsahuje údaje účtu ani klientů. Funguje také pouze v paměti, pokud prohlížeč úložiště blokuje. Změna se synchronizuje mezi záložkami.

Na šířkách pod 1024 px zůstává samostatné mobilní menu s celými názvy. Má ovládání Escape, udržení a návrat zaměření klávesnice, blokování posuvu pozadí a automatické zavření při přechodu na desktop. Dlouhá navigace má vlastní posuv; odhlášení zůstává dostupné. Animace respektují omezený pohyb systému.

## Ověření

- 1 800 testů / 192 souborů prošlo; 8 nových testů pokrývá ukládání a obnovu volby, synchronizaci, blokované úložiště, oprávnění, rozpracované sekce, odhlášení, popisky, mobilní klávesnici a vložený režim.
- ESLint bez chyb i varování. Produkční sestavení včetně TypeScriptu prošlo. Kontroly obrazového runtime a registrace autentizačního proxy prošly.
- Izolovaný Chrome: desktop 1440 px, nižší okno 1024 × 600, mobil 390 × 844 a 320 × 568. Ověřena skutečná změna šířky, obnovení stránky, viditelnost odhlášení, popisek a klávesnice, změna velikosti okna i absence vodorovného přetékání.
- Vizuální náhled používal smyšlené údaje a blokované externí požadavky. Dočasný náhled byl před finálním sestavením odstraněn a původní stránka `/ucet/akce` obnovena beze změny.

## Rozsah nasazení

Izolovaná kopie produkční verze `dpl_48bcUawQaeF8e24MPLf5q6qQr9nz`. Změna obsahuje pouze `AppNavigation.tsx`, `appNavigation.module.css`, `useSidebarPreference.ts` a test navigace. Soukromé prostředí, dokumentace a místní náhledy nejsou v balíčku. Produkční parametry a region Frankfurt zůstávají zachované.

Podrobnosti kontrol a potvrzení nasazení jsou v sousední složce `sidebar-2026-09-13/`.

Nasazeno na `https://bohemka.app` ve 13:36 CEST: `dpl_FK81FV8LqN1GtKUuYeQdui8zuaC6`. Hash všech 1 249 nasazených souborů odpovídá otestované kopii; oproti předchozí verzi se změnily výhradně čtyři soubory navigace. Všech devět kontrol před přepnutím domény prošlo.

Po zveřejnění prošlo stejných devět kontrol také na hlavní doméně. Nebyla použita aplikační přihlášení, čtena klientská data ani odeslán e-mail.
